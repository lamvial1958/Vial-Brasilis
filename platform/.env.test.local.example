# Credenciais de um aluno de teste (conta real no Firebase — não use a conta admin)
# Copie este arquivo para .env.test.local e preencha com os valores reais.
# .env.test.local já está no .gitignore e nunca será commitado.

TEST_USER_EMAIL=aluno-teste@exemplo.com
TEST_USER_PASSWORD=senha_aqui

# Opcional: URL base alternativa (padrão: http://localhost:3000)
# PLAYWRIGHT_BASE_URL=https://platform-henna-nine.vercel.app
